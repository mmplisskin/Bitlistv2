<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- <a class="navbar-brand" href="/"><i class="fa 4x fa-bitcoin b"></i>itlist</a> -->
      <div id="pay-with-btc" class="navbar-brand">
        <a href="/">
          <div id="shine"></div>
          <div id="logo">
            <div id="coin">
              <center>
                <i class="fa fa-btc logoBTC"></i>
              </center>
            </div>
            <div class="logo-text">itlist </div>
          </div>

        </a>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
          <li><a href="/static_pages/about">About</a></li>
          <% if current_user %>
          <li><a href="/users/<%=current_user.id%>"><i class="fa fa-user"></i> Account </a></li>


          <% else %>
          <li><a href="/auth/facebook" data-toggle="modal" data-target="#myModal">Login</a></li>
          <% end %>
      </ul>

      </form>
        <form class="navbar-form navbar-left" action="/items/new" >
          <button type="submit", class="btn btn-primary"><i class="fa fa-plus"></i> Post</button>
        </form>

        <div class="nav navbar-nav navbar-left rate">

          Average rate:

          <br>
          <i class="fa fa-dollar"></i>
          <%= current_rate %>

        </div>

        <div class="nav navbar-nav navbar-right">
          <% if current_user %>
          <li> <%= link_to '<i class="fa fa-sign-out"></i> Log Out'.html_safe, logout_path, method: :delete, class: 'btn btn-hidden'%></li>
          <% end %>

        </div>

      <%= form_tag search_items_path, class:"navbar-form navbar-right", method: :get do |f| %>
      <div class="form-group">
        <%= text_field_tag :search, nil, placeholder: 'search', class:"form-control" %>
        <%= text_field_tag :location, nil, placeholder: 'near', class:"form-control" %>
      </div>
       <button type="submit" class="btn btn-default">Search <i class="fa fa-search"></i></button>

       <%#= submit_tag '>' %>
     <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
