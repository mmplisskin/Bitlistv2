
<%if @items && @items.length > 1%>

  <%= render '/layouts/items' %>

<%else flash.notice = " :( Sorry nothing matches your search criteria.. Please try again. "%>

<%end%>
  <%=link_to "All categories", root_path%>
