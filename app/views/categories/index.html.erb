<div class="panel panel-body">
  <% @categories.each do |category| %>

    <%# link_to category.name, category_path(category.id) %>
    <div class="well" id="categoryNAMES">

      <h1><%= category.name %></h1>
      <a href="/categories/<%=category.id%>"> </a>
    </div>

  <% end %>
</div>
